<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="l10n_sv_view_account_journal_form" model="ir.ui.view">
        <field name="name">l10n_sv_extra.view_account_journal_form</field>
        <field name="model">account.journal</field>
        <field name="type">form</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Extra SV" name="extra_sv">
                    <group string="Configuración SV">
                        <group>
                            <field name="direccion" invisible="type not in ['sale', 'purchase']"/>
                            <field name="resolucion" invisible="type not in ['sale', 'purchase']"/>
                        </group>
                        <group>
                            <field name="facturas_por_rangos" invisible="type not in ['sale']"/>
                            <field name="serie" invisible="type not in ['sale', 'purchase']"/>
                            <field name="rango_inicio" invisible="type not in ['sale']"/>
                            <field name="rango_fin" invisible="type not in ['sale']"/>
                        </group>
                    </group>
                </page>
            </notebook>
        </field>
    </record>

</odoo>
